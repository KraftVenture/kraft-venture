# Supabase Row Level Security (RLS) Policies

This directory contains RLS policy definitions for database tables.

## Structure

- Policies are organized by table or feature
- Example: `users_policies.sql`, `projects_policies.sql`

## Usage

Policies can be applied via migrations or directly through the Supabase dashboard.

