# Supabase Edge Functions

This directory contains Supabase Edge Functions (serverless functions).

## Structure

- Each function should be in its own directory
- Example: `send-email/`, `process-payment/`

## Usage

```bash
# Deploy a function
supabase functions deploy function_name

# Test locally
supabase functions serve function_name
```

